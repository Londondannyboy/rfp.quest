#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run build before push to prevent broken deployments
echo "Running build check before push..."
npm run build

if [ $? -ne 0 ]; then
  echo "❌ Build failed! Fix the errors before pushing."
  exit 1
fi

echo "✅ Build successful! Proceeding with push..."